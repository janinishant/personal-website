$("#menu-close").click(function (a) {
    a.preventDefault();
    $("#sidebar-wrapper").toggleClass("active")
});
$("#menu-toggle").click(function (a) {
    a.preventDefault();
    $("#sidebar-wrapper").toggleClass("active")
});
$(function () {
    $("a[href*=#]:not([href=#])").click(function () {
        if (location.pathname.replace(/^\//, "") == this.pathname.replace(/^\//, "") || location.hostname == this.hostname) {
            var a = $(this.hash);
            a = a.length ? a : $("[name=" + this.hash.slice(1) + "]");
            if (a.length) {
                $("html,body").animate({scrollTop: a.offset().top}, 1000);
                return false
            }
        }
    })
});
time = new Date().getTime();
$(window).scroll(function (c) {
    var d = new Date().getTime();
    var a = $(window).scrollTop();
    if (a < 700) {
        var b = (a / 300);
        $(".scroll-img").css("opacity", b);
        $(".location-label").css("opacity", 0.2 + Math.min(b / 2, 0.4));
        time = d
    }
});
$(document).ready(function () {
    var h = {Bombay: "Bombay, India", LA: "Los Angeles"};
    var a = Object.keys(h);
    var f = Math.floor(Math.random() * a.length);
    var d = a[f];

    var g = "assets/img/bg/"+d+".jpg";
    var i = "assets/img/bg/"+d+"-blur.jpg";
    var b = "url(" + g + ")";
    var e = "url(" + i + ")";
    $(".img-src").css("background-image", e);
    $(".scroll-img").css("background-image", b);
    $(".location-label").text(h[d]);


});
$(".icon").tooltip({placement: "bottom", delay: 100});